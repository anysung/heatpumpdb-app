{
  "//": "Test-only Firebase config for the emulator rules suites (npm run test:rules / test:rules:storage). NOT used by any deploy — deploys use firebase.json. Kept separate because firebase.json's storage config is a multi-bucket deploy array the emulator cannot boot from.",
  "firestore": { "rules": "firestore.rules" },
  "storage": { "rules": "storage.rules" },
  "emulators": {
    "firestore": { "port": 8080 },
    "storage": { "port": 9199 },
    "ui": { "enabled": false }
  }
}
