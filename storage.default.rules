rules_version = '2';
// Default bucket — unused by the app; locked down completely.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
